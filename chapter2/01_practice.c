#include <stdio.h>
#include <math.h>

int main()
{

    int a = 8,b = 4;
    float x = 8, y = 4;
    // printf("Enter value of a ");
    // scanf("%d",&a);

    // printf("Enter value of b ");
    // scanf("%d",&b);

    printf("The value of a + b is %d \n", a + b);
    printf("The value of a - b is %d \n", a - b);
    printf("The value of a * b is %d \n", a * b);
    printf("The value of x / y is %f \n", x / y);
    printf("The value of a to the power b is %f", pow(a, b));
    return 0;
}