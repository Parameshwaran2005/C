#include<stdio.h>

int main(){


     int principal=98000,years=1;
    float rate=8.1;
    float simpleinterest = (principal*rate*years)/100;

     printf("The principal interest is %f", simpleinterest);
     return 0;

}